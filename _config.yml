title: SeatShare
email: contact@myseatshare.com
description: >
  SeatShare is the easiest way to manage your shared pool of tickets for a
  sports team or performing arts venue.
baseurl: ""
url: "https://www.myseatshare.com"
twitter_username: myseatshare
github_username: seatshare

exclude: [ Gemfile, Gemfile.lock, Procfile, vendor, gems, config, Rakefile, CNAME, .buildpacks, circle.yml ]

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
